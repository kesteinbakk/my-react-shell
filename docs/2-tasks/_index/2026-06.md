# Tasks — my-react-shell · 2026-06

Source of truth for task IDs in this project for this month. Lives in this project's repo under `docs/2-tasks/_index/`. Edited only in the primary working tree and committed immediately, so worktrees and branches can't diverge the numbering — see `~/Developer/CLAUDE.md` → Task & Bug Index.

## Rules

- **New rows go on top. Never reorder existing rows.** The top row is therefore always the highest `TXXX` — read it alone to get the next ID (`top + 1`), no need to scan the file.
- **At month rollover** (you are creating this file fresh for a new month): open the *previous* month's file, copy every still-open task row into this file, and report the carry-forward to the user. Number the first new task of the month from the previous month's top row `+ 1` (a closed top item stays behind but still counts).
- **Description = 1–8 sentences** — a brief overview of what the task is about. The full writeup lives in `docs/2-tasks/TXXX-.../task.md`, not here.
- **Status:** `planning → in-progress → finished → archived`. Also `blocked` (waiting on external resolution), `on-hold` (intentionally parked), `cancelled` (append `(cancelled)` to the description; row stays — never renumber). Bump to `in-progress` and fill the `Worktree` column immediately before spawning the first subagent or starting impl — see the `tm` agent for the trigger rule.

| T-ID | Name | Status | Worktree | Description | Affects |
|------|------|--------|----------|-------------|---------|
| T007 | typed-i18n-keys | in-progress | main | **Filed 2026-06-20.** Lift compile-time key safety into the shell's i18n seam (`my-react-shell/i18n`), opt-in and **non-breaking** — every public type/hook gains a key type param defaulting to `string`, and the runtime (provider, context, pure translator, missing-key store) is **unchanged**. Today the seam is stringly-typed (`TFunction = (key: string) => string`); a typo / missing key is caught only at runtime via the dev `<MissingTranslationsOverlay>`. evaluering (already a `link:` consumer) independently solved this with a hand-rolled `DictKey` union + `en: Dict` parity, so the ecosystem's strongest i18n property lives in one consumer while the shared hub is weaker than what depends on it — any consumer adopting the shell seam would *lose* compile-time safety. Make typed keys the shell standard: ship `createTypedI18n<K>()` (a typed factory — pure typing sugar over generic `useTranslation`/`translateNow`, **no new runtime/context**), make `TFunction`/`I18nContextValue`/`useTranslation`/`translateNow` generic over `K extends string = string`, and export a `DotPaths<T>` utility so nested-catalog consumers can derive the union. A consumer declares `const { useTranslation, translateNow } = createTypedI18n<DictKey>()` once and every call site is typed with zero per-call ceremony. **Supersedes the evaluering Option-B shim plan** — evaluering binds its `DictKey` to the shell hook directly (no local casts), keeping its **64 `useT()` + ~10 `useLanguage`/toggle sites untouched**; that adoption is consumer-side, **filed as evaluering T032** (blocked on this shipping). Sub-decision **decided 2026-06-20 → A** (factory binds a key union; `DotPaths<T>` exported for nested catalogs); new decision **D14**. Full plan: `docs/2-tasks/T007-typed-i18n-keys/task.md`. | `src/i18n/`: `i18nContext.ts` + `useTranslation.ts` + `translateNow.ts` + `translate.ts` (generics + `DotPaths<T>`), **new** `createTypedI18n.ts`, `index.ts` exports, `I18nProvider.tsx` (optional `messages` typing); typed-key `*.test-d.ts`; `docs/guides/i18n.md` (Typed keys section); `react-translation-rules` skill; `strategy.md` (D14); `concept.md`; `CLAUDE.md` (if seam desc changes); `my-react-shell-demo` i18n showcase. **Consumer-side (separate repo/task):** evaluering `src/i18n/index.ts`. |
| T006 | shared-theme-package | in-progress | main | **Filed 2026-06-20. Code complete + verified locally; committed in all 3 repos** (my-react-shell `tsc -b` + Vite build green — palettes resolve from `themes`, `golden` correctly excluded; foundation `tsc` green + all 7 `@import`s resolve; pre-commit guard passed). **Pending user:** create the Bitbucket remote, then push + **tag `themes` `v0.1.0`** and `pnpm install` in each consumer (until then the committed git-dep specifier resolves only via the local symlink). End theme/color drift between my-react-shell (React) and `zingularis/foundation` (SolidJS) by extracting the semantic token contract + palettes into a new **framework-neutral git-dep package `themes`** (`~/Developer/themes`, `git@bitbucket.org:kesteinbakk/themes.git`) that both consume. Ships `contract.css` (the superset `:root` `--color-*` contract, incl. the `-border`/`share-border` tokens foundation's `:root` was missing), the 5 shared palettes (`ocean/forest/sunset/soft/dynamic`, already byte-identical across both repos) + `golden`, an `index.css` barrel, and a `palettes.json` manifest — **pure CSS/JSON, no build**. Both repos `@import` from `themes` and add it as a regular `dependencies` git-dep (transitive to their own consumers); each keeps its own framework-specific `ThemeProvider` and its own small baseline element styles (foundation's are tangled with app utility classes + fretboard animations, which stay put). Selected approach **A** over a sync-script (B). Supersedes **D4** ("foundation untouched") via new **D13**. Distribution = Bitbucket git-dep per D5; local dev via symlink/link, the committed specifier is the tag-pinned git-dep. **Remote creation + push/tag = user-owned.** Full plan: `docs/2-tasks/T006-shared-theme-package/task.md`. | New repo `~/Developer/themes` (`contract.css`, 6 palettes, `index.css`, `palettes.json`, `package.json`, `README.md`); my-react-shell `src/index.css` + `src/styles/base.css` (split contract out) + delete `src/styles/themes/*` + `package.json` (dep + `files`) + `strategy.md` (D13) + `concept.md` + `CLAUDE.md` + memory; foundation `src/themes/*` (base.css split, swap palette imports) + `package.json`. |
| T005 | user-preferences | in-progress | main | **Filed 2026-06-20. Code complete** — lib + demo typecheck green, committed; **pending** in-browser verification (modal opens, palette/mode change live, emoji toggle flips every shell icon — user-owned dev server). A default **user-preferences** component for the kit plus an **icon/emoji display-mode** seam, inspired by foundation's `ThemeAction` modal (`shell/actionRow.tsx`) + `kit/icons/Icon.tsx`. New module `my-react-shell/icons`: `IconModeProvider` (uncontrolled+localStorage by default, controllable via `value`+`onChange` to redirect to a backend), `useIconMode`, and a thin `<Icon icon emoji>` swap that renders an SVG glyph or its emoji per the active mode — **no baked-in registry and no `lucide-react` dep** (consumer brings keys; the demo builds a `renderIcon` registry on top). New component `my-react-shell/components` → `<UserPreferences>`: a **controlled** theme/display panel (palette grid + light/dark/system + icons↔emojis) in a Radix **dialog** (reuses the existing `@radix-ui/react-dialog` peer — no popover dep), exposing the value + `onChange` for every preference so the consumer owns persistence (localStorage / Convex / account); optional `accountActions` slot; labels via props (no runtime i18n); auth-free + Convex-free. Additive non-breaking `onChange` on `ThemeProvider`. Demo: `IconModeProvider` wrap, chrome swaps its `PaletteSelect`/`ThemeModeToggle` for `<UserPreferences>`, and `renderIcon` honors the mode so the whole demo flips icons↔emojis. **Decisions:** Q1→seam+thin-Icon (not a full Icon module); Q2→preferences-only (auth via optional slot); component is controlled (project chooses storage). Full plan: `docs/2-tasks/T005-user-preferences/task.md`. | `src/icons/` (new), `src/components/UserPreferences.tsx` + `components.css` + `index.ts`, `src/theme/ThemeProvider.tsx`, packaging (`exports`, `CLAUDE.md`), `concept.md`, `strategy.md` (D12), `docs/guides/` (new icons guide + component-kit entry); demo `main.tsx`, `chrome.tsx`, `shell-config.tsx`, `pages/`. |
| T004 | component-kit | finished | main | **Filed 2026-06-20.** **Phases 1–4 done** (charter D11 + `components` module; P1 `Alert`; P2 feedback: `Spinner`/`PageSpinner`/`SectionSpinner`, `InfoBox`, `EmptyState`, `ConfirmDialog`, `Toast`; P3 data-display: `Badge`, `Chip`/`ChipGroup`, `Avatar`/`AvatarGroup`, `Table` — `Card` left to shadcn per the rule; P4 forms: `InputField`, `SegmentedControl`, `Select` (+ `@radix-ui/react-select` optional peer); plus the dark-shadow-token elevation fix; all in the demo Kit route; lib+demo typecheck green). **Closed complete** — the app-shell CVA-drift align was declined (cosmetic; would couple app-shell→components or duplicate the helper). Ship an **opinionated component kit** at `my-react-shell/components` — React composites that bake design/layout/behavior decisions on shadcn/Radix primitives and render against the semantic theme tokens (light+dark, every palette), mirroring `@foundation/kit`. **Reverses the "no component kit" charter** (concept "What it is NOT", strategy D3/D8, CLAUDE.md) via a new decision D11, following the D10 app-shell precedent (component module behind a sub-path with optional Radix peer). **Scope line:** ship only components that need an *opinion*; un-opinionated shadcn primitives (Button/Input/Checkbox/…) stay consumer-owned and are shown as demo examples only. Proposed set: Alert, ConfirmDialog, Toast, InfoBox, EmptyState, Spinner, Card, Badge, Chip, Avatar, Table, InputField, Select, SegmentedControl — phased, **Alert first**. All showcased in a new **Kit route** in `my-react-shell-demo` (landing card + page + nav, mirroring the existing Components/Palette pages). Optional peers (Radix, CVA, tailwind-merge, clsx) approval-gated at install. Full plan: `docs/2-tasks/T004-component-kit/task.md`. **Refs:** foundation `src/kit/`, strategy D3/D8/D10, the secondary+`-strong` token work. | `src/components/` (new), packaging (`exports`, optional peers, `tsconfig.lib.json`), `concept.md`, `strategy.md`, `CLAUDE.md`, `README.md`, `docs/guides/component-kit.md` (new); demo `pages/KitPage.tsx`, `router.tsx`, `HomePage`, `shell-config.tsx`, `sections/`. |
| T003 | app-shell-module | in-progress | main | **Filed 2026-06-17.** React (SPA) port of the SolidJS `foundation` app shell as an optional `my-react-shell/app-shell` module. Ships the shell chrome (`AppShell` header-or-sidebar mode + responsive mobile drawer + optional bottom nav + the single scrolling body cell), `ShellPageHeader` (URL-derived breadcrumbs + actions + search + `subPages` title dropdown), `defineShellConfig` (pages tree + the three navigation layers), and the page-tab primitives (`PageSections` in-page `?tab=` with single/list-scrollspy/collapsible + route-based `PageTabs`). **TanStack Router** is an *optional* peer behind the sub-path (amends D2); **Radix** headless primitives for Sheet/Drawer, DropdownMenu, Popover. Display strings via config/props — no hard i18n dep. Excludes app-glue (notification system, feedback modal, command-bar/action registry). From the approved proposal `docs/1-proposals/app-shell-module.md`; sequenced **after** the i18n module. **Code complete** — all phases implemented (spine + chrome + page header + page tabs + packaging + guide); `tsc -b` + `pnpm build:lib` green, 13 config-validator checks pass. **Pending:** browser-behavior verification + harness playground routes (a new route needs a dev-server run to regenerate `routeTree.gen.ts` — user-owned). **Refs:** foundation `src/shell/` + `docs/guides/app-shell-rules.md`, strategy D2/D8/D9. | `src/app-shell/` (new), packaging (`exports`, optional peers), `docs/guides/app-shell.md` (new), harness routes `src/routes/**`, `concept.md`, `strategy.md`. |
| T002 | dedupe-link-dev-loop | finished | main | **Filed 2026-06-16.** De-risk the `link:` dev-loop against duplicate-React crashes. A consumer following the documented local `link:` loop hits a hard `Invalid hook call` crash at first paint: the symlinked shell drags in its own `node_modules/react`, so the consumer's bundler resolves two physically distinct React copies and the hook dispatcher splits. Fix is consumer-side bundler dedupe (Vite `resolve.dedupe`), baked into the reference consumer configs and documented next to the existing peers / Tailwind / strip-the-link steps. The shell keeps its own React (dev-harness/build/tests need it), so this is document-and-encode, not package-level elimination. Scoped to the `link:` loop only — the tag-pinned git-dep path dedupes via peers and is unaffected. From `docs/1-proposals/react-dedupe-link-dev-loop.md`. | `docs/guides/distribution-model.md`, `README.md`, `CLAUDE.md`; consumer configs `evaluering/vite.config.ts`, `my-react-shell-demo/vite.config.ts`. |
| T001 | modular-foundation | in-progress | — | **Filed 2026-06-15.** Stand up my-react-shell as a **modular React foundation**: optional, self-contained **drop-in modules** (theme, providers, auth seam, i18n) an app imports à la carte, consumed like a standard npm package from a tag-pinned Bitbucket git-dependency (a `prepare` build compiles `src/ → dist/`). **No** component kit, **no** shadcn registry, **no** mandated app-shell. Each module is independently importable, self-contained, and ships a contract + guide; modules contributed on rule-of-two reach every app on version bump. Done: scaffold, theme, providers + auth seam, distribution, **i18n module** (`my-react-shell/i18n`), **module-authoring + per-module guides**, `core` left empty-by-design (rule-of-two for types). Remaining: the v0.1.1 distribution zero-config-install decision (build sequence D2 item 5 — owner call) + harness showcase routes (need a dev-server run to regenerate `routeTree.gen.ts`). Supersedes the original "port foundation-react / host a shadcn registry / build the app-shell" scope (strategy D8). Blocks every my-react-shell consumer. **Refs:** strategy D8, react-framework-notes. | `src/theme/`, `src/providers/`, `src/auth/`, `src/core/` (new), `src/i18n/` (new), packaging (`tsconfig.lib.json`, `exports`), `docs/guides/`. |
