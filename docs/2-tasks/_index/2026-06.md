# Tasks — my-react-shell · 2026-06

Source of truth for task IDs in this project for this month. Lives in this project's repo under `docs/2-tasks/_index/`. Edited only in the primary working tree and committed immediately, so worktrees and branches can't diverge the numbering — see `~/Developer/CLAUDE.md` → Task & Bug Index.

## Rules

- **New rows go on top. Never reorder existing rows.** The top row is therefore always the highest `TXXX` — read it alone to get the next ID (`top + 1`), no need to scan the file.
- **At month rollover** (you are creating this file fresh for a new month): open the *previous* month's file, copy every still-open task row into this file, and report the carry-forward to the user. Number the first new task of the month from the previous month's top row `+ 1` (a closed top item stays behind but still counts).
- **Description = 1–8 sentences** — a brief overview of what the task is about. The full writeup lives in `docs/2-tasks/TXXX-.../task.md`, not here.
- **Status:** `planning → in-progress → finished → archived`. Also `blocked` (waiting on external resolution), `on-hold` (intentionally parked), `cancelled` (append `(cancelled)` to the description; row stays — never renumber). Bump to `in-progress` and fill the `Worktree` column immediately before spawning the first subagent or starting impl — see the `tm` agent for the trigger rule.

| T-ID | Name | Status | Worktree | Description | Affects |
|------|------|--------|----------|-------------|---------|
| T021 | table-row-interaction-expandable | in-progress | main | **Filed 2026-06-23.** Implement proposal `table-row-interaction-and-expandable-rows.md` (evaluering T063 remove-shadcn gap). Extend the kit **`Table`** with three additive affordances: **(1) `onRowClick(row, index)`** — whole-row click with a pointer/hover affordance, guarded so clicks originating from interactive cell content (`button`/`a`/`input`/`select`/`textarea`/`label`/`[role=button]`/the disclosure toggle) don't fire the row handler; **(2) `renderExpanded(row)`** — kit owns per-row open state and renders a leading disclosure toggle column; the consumer supplies the detail node, rendered in a full-width `colSpan` row directly below, collapsible per row; **(3) `rowVariant(row, index)`** → `default`·`muted`·`selected`, themed via `--color-*` tokens (no hardcoded colors). Dynamic (render-time) columns already work with the existing `columns` array API — showcase only, no API change. All controlled/consumer-driven, consistent with the kit. Showcased on `my-react-shell-demo` TablesCardsPage + API reference. | `src/components/Table.tsx`, `components.css` (`.mrs-table` row-interaction/expand/variant), `dist/` rebuild, `docs/specifications/api-reference.md`; `my-react-shell-demo` `src/pages/TablesCardsPage.tsx`. |
| T020 | dropdown-submenu-selectable | in-progress | main | **Filed 2026-06-23.** Implement proposal `dropdown-menu-submenu-and-selectable-items.md` (evaluering T063 remove-shadcn gap). Extend the kit **`DropdownMenu`** `items` union with three new entry types via Radix's native primitives: **(1) `checkbox`** (`checked` + `onCheckedChange`, independent toggle) on `RadixMenu.CheckboxItem`; **(2) `radio-group`** (`value` + `onValueChange` + `options`, one-of-a-group) on `RadixMenu.RadioGroup`/`RadioItem`; **(3) `submenu`** (`label`/`icon` + nested `items`, recursive arbitrary depth) on `RadixMenu.Sub`/`SubTrigger`/`SubContent`. Checkbox/radio items keep the menu open by default (per-item `closeOnSelect` override); plain items still close. Checked/selected state fully consumer-controlled, consistent with the rest of the kit's controlled inputs. Additive/non-breaking. Showcased on `my-react-shell-demo` InputsActionsPage (DropdownMenu section) + API reference. | `src/components/DropdownMenu.tsx`, `components.css` (`.mrs-menu` checkbox/radio/submenu indicators), `dist/` rebuild, `docs/specifications/api-reference.md`; `my-react-shell-demo` `src/pages/InputsActionsPage.tsx`. |
| T019 | count-pill-badge-attrs | finished | main | **Filed 2026-06-23.** Add a general **`CountPill`** kit component (`my-react-shell/components`) — a solid-fill numeric count pill (`tone` primary/secondary/success/warning/danger/info, clamps at `max`, default 99 → `99+`, `tabular-nums`), the missing primitive consumers were faking by forcing the status `Badge` into a circular overlay (evaluering `NotificationBell`'s unread bubble used `absolute … size-4 rounded-full px-0 …` overrides). Foundation ships this nav-coupled as `NavBadge`; the shell version is deliberately general/un-coupled, not a nav port (per user). Also make **`Badge`** forward standard `<span>` HTML attributes (`ComponentPropsWithoutRef<'span'>`: `title`, `aria-*`, `data-*`, `id`, events) so a consumer attaches a native tooltip etc. without a wrapper `<span>` (motivated by evaluering's "below cutoff" badge). Both additive/non-breaking; uses `tone` to align with T018's prop unification. Showcased on `my-react-shell-demo` TagsAvatarsPage + API reference. | `src/components/CountPill.tsx` (new), `Badge.tsx` (span attr spread), `components.css` (`.mrs-count-pill*`), `index.ts` (export), `dist/` rebuild, `docs/specifications/api-reference.md`; `my-react-shell-demo` `src/pages/TagsAvatarsPage.tsx` + `src/shell-config.tsx` icon. |
| T018 | complete-component-surface | finished | zen-shannon-7abaec | **Filed 2026-06-23.** Re-found the project's thesis: my-react-shell becomes a **complete, opinionated support + starting base for React projects** — the React counterpart to what the SolidJS `foundation` is for Solid — rather than a light "menu of modules that leaves primitives to the consumer." **Drop shadcn entirely.** It was never a library dependency: every kit component already builds directly on `@radix-ui/*` + `cva` + `mrs-` CSS + the `--color-*` tokens; shadcn only ever entered as the *consumer's* copy-in source for un-opinionated primitives. Ship those primitives ourselves in the same Radix idiom so a consumer imports the whole surface from `my-react-shell/components` and adds no shadcn — plain-element+cva (`Button`, `Input`, `Textarea`, `Label`, `Card`, `Separator`, `Skeleton`) and Radix-wrapped (`Checkbox`, `Switch`, `RadioGroup`, `Tooltip`, `Tabs`, a general `Dialog`; plus *exposing* the `DropdownMenu`/`Popover` already wrapped internally). This eliminates (a) the shadcn cssVar bridge consumers hand-roll — primitives now render against `--color-*` directly; (b) same-name collisions (the evaluering `Badge` confusion — one `Badge` now); (c) the "which source?" decision per component. **Unify the prop convention:** `tone` for semantic color across the whole kit (move `Alert`/`ActionButton`/`ConfirmDialog` off `variant`-for-color onto `tone`), reserve `variant` for structural style only. **Docs:** delete `strategy.md` (the Dx decision/rule log) outright and re-found the charter in a rewritten `concept.md`; then purge every dangling `strategy.md` / `Dx` / "use shadcn directly" reference across `CLAUDE.md`, `README.md`, `demo.md`, `guides/distribution-model.md`, `guides/theme.md` (drop the shadcn-bridge section), and `specifications/api-reference.md`. Full plan + open decisions: `docs/2-tasks/T018-complete-component-surface/task.md`. | `src/components/*` (new primitives + `tone` unification, `components.css`, `index.ts`), `package.json` (Radix peers for new wrapped primitives), `dist/` rebuild, docs (**delete** `strategy.md`; rewrite `concept.md`; sweep `CLAUDE.md`, `README.md`, `demo.md`, `guides/distribution-model.md`, `guides/theme.md`, `specifications/api-reference.md`), the `my-react-shell` skill card; `my-react-shell-demo` (primitive showcase, kit pages, `/components` page now kit-owned); consumer `evaluering` (drop `components/ui`, repoint imports, remove the cssVar bridge). |
| T017 | page-header-hook-auto-band | finished | quirky-perlman-5a8c08 | **Filed 2026-06-22. Supersedes T016.** Redesign the app-shell page-header API. **(1)** Replace the render-null `<ShellPageHeader>` **component** with a **`usePageHeader({ title, actions, search, tabs, documentTitle, className })` hook** — clean break, no compat shim. **(2)** Make the **breadcrumb band render automatically** (from the URL chain / registered page chrome / mobile-menu button — lifting foundation's existing `showBand()` content-gate up to be *the* gate) so a page no longer mounts an empty header just to show breadcrumbs; evidence the old coupling was a foot-gun: the demo mounts 12 bare `<ShellPageHeader/>` and passes props on none. **(3)** Add a per-`PageEntry` **`hideCrumb?: () => boolean`** predicate that omits an access-gated level from the rendered trail while keeping it structurally (URL + descendants intact); the consumer supplies the access logic, the shell stays role-agnostic (auth-seam philosophy). Internally keeps T016's register-once + in-place update and resolves the winning chrome by **render-order — deepest-mounted wins** (matches foundation's parent-first `onMount`; kills the last-writer flicker that React's child-first effects caused), and **drops** the now-misleading "multiple mounted" dev warning. All three are **deliberate divergences from the Solid foundation** (it has neither a hook nor `hideCrumb`; it gates the band on a registered spec) — recorded in strategy.md. Breaking + fully documented + every consumer migrated in this task: rewrite **evaluering** (4 layout shells drop bare mounts but keep `useDynamicPages`; ~14 views `<ShellPageHeader actions>`→`usePageHeader`; 10 tests; strip ~18 last-writer-wins comment blocks; rewrite `evaluering/docs/guides/app-shell-structure.md`) and **both demos** (remove the 12 bare mounts; add a real `usePageHeader` actions/search/tabs showcase + a `hideCrumb` example; theme-demo `title`→hook). Full plan: `docs/2-tasks/T017-page-header-hook-auto-band/task.md`. | `src/app-shell/` (`shellContract` `hideCrumb`+`PageHeaderOptions`; `shellContext` render-order winner; new `usePageHeader.ts`; `ShellPageHeader.tsx` remove component + `hideCrumb` render-skip; `AppShell.tsx` auto band-visibility; `index.ts` barrel), `dist/` rebuild, docs (`api-reference.md`, `guides/app-shell.md`, `CLAUDE.md`, `strategy.md`, skill `SKILL.md`, `README.md`); `evaluering` (shells/views/tests + its app-shell-structure guide); `my-react-shell-demo` + `my-react-shell-theme-demo`. |
| T016 | page-header-register-once | superseded | quirky-perlman-5a8c08 | **Superseded by T017** (the narrow register-once fix is folded in as T017's internal descendant-wins tiebreak; not shipped on its own). **Filed 2026-06-22.** Make the React `<ShellPageHeader>` registration deterministic — the **real fix** for the recurring "the shell's header stack is last-writer-wins" surprise that trips agents on every header task (origin: user question on why the React shell raises this where the SolidJS `foundation` never did). Both shells use an identical last-mounted-wins stack (`headerStack.at(-1)`), but Solid registers **once** in `onMount` with a stable getter-backed spec, so stack order is frozen at mount order and prop changes flow through the getters without re-registering — deterministic and invisible. The React port instead registers in a `useEffect` **keyed on the thunk props** (`title`/`actions`/`search`/`tabs`/`documentTitle`/`className`), so unstable inline thunks (the idiomatic call shape, `actions={[() => <Btn/>]}`) make the effect cleanup + **re-push** on every render; with two headers mounted at once (layout + page) the winner becomes "whichever re-rendered last", not "whichever mounted last" — it can flip at runtime with no change to the mount tree. **Fix = split identity from content:** register the slot **once** on mount (stable deps) to fix its stack position, then update its spec **in place** (no re-push) on prop change via a returned `{ update, unregister }` handle, the spec reading current props through a ref. Result: stack order == mount order, stable — matching foundation. Solid's reactive getters can't be ported literally, so this is the idiom-level React equivalent (CLAUDE.md → faithful port = idiom changes, behavior preserved). Also **restores foundation's dropped dev `console.warn`** when two `<ShellPageHeader>`s mount simultaneously. Internal-only — `registerPageHeader`'s return type changes (sole caller is `<ShellPageHeader>`); no public-API / `<ShellPageHeader>` prop change. Full plan: `docs/2-tasks/T016-page-header-register-once/task.md`. | `src/app-shell/ShellPageHeader.tsx` (register-once mount effect + in-place update effect + ref-backed spec), `src/app-shell/AppShell.tsx` (`registerPageHeader` returns `{ update, unregister }`; stack holds id+spec; restore dev warning), `src/app-shell/shellContext.ts` (`registerPageHeader` return-type — internal), `dist/` (rebuild), `docs/guides/app-shell.md` (page-header section: one-per-page + determinism note). |
| T015 | collapsible-accordion | finished | main | **Filed 2026-06-22.** Two new opinionated kit composites for the **disclosure** family (`my-react-shell/components`), requested by the evaluering consumer. **(1) `Collapsible`** — a faithful React/Radix port of the SolidJS `foundation` `Collapsible` (`src/kit/shared/Collapsible.tsx`): single trigger toggling one region, controlled (`expanded`/`onExpandedChange`) + uncontrolled (`defaultExpanded`), static `trigger` or `renderTrigger(expanded)`, rotating chevron (`showArrow`), four trigger variants (`default`/`bordered`/`ghost`/`filled`), three sizes, `inlineChevron` (chevron after content vs pushed to right edge), `disabled`, `animationDuration`, and the height animation. Kobalte→Radix is the only idiom change; feature set matches foundation. **(2) `Accordion`** — net-new opinionated composite on `@radix-ui/react-accordion` (foundation ships none): data-driven `items` list, `type` single/multiple, `collapsible`, controlled/uncontrolled `value`/`defaultValue`/`onValueChange`, `variant` (`default`/`bordered`/`separated`), `size`, `showArrow` — gets roving arrow-key focus + exclusive/multi-open from Radix. **Two new optional peers** behind `/components` (user-approved): `@radix-ui/react-collapsible`, `@radix-ui/react-accordion`. Showcased on `my-react-shell-demo` (a new disclosure section/page) + documented in the API reference. | `src/components/Collapsible.tsx` + `Accordion.tsx` (new), `src/components/components.css` (`.mrs-collapsible*` + `.mrs-accordion*` + keyframes), `src/components/index.ts` (exports), `package.json` (two Radix optional peers + devDeps), `docs/specifications/api-reference.md` (component rows + subsections + peers/surfaces); `my-react-shell-demo` kit page + `shell-config.tsx` icons; `dist/` rebuild. |
| T014 | breadcrumb-overflow-collapse | finished | main | **Filed 2026-06-22.** Fix the breadcrumb band exploding to multiple wrapped lines when the trail is deep and/or a label is long (reported with a 4-level chain + an 80-char dynamic leaf). **Two fixes, both net-new vs the SolidJS `foundation` (foundation only `truncate`s; user-approved divergence):** (1) **per-crumb hard cap + ellipsis** on *every* crumb (links, leaf, leaf-dropdown trigger) — dynamic record names appear at any level, not just the leaf, so all are capped — via a CSS `max-width` custom prop, with `flex-shrink:0` on home + chevrons and `white-space:nowrap` so the row never wraps; (2) **middle-collapse** — when `chain.length > leading + trailing` (default `leading:1`, `trailing:2`), render the first `leading` crumbs, a single **"…" overflow item** (a Radix `DropdownMenu` listing the hidden ancestor crumbs, navigable, reusing the existing `LeafDropdown` machinery + the existing `labels.more` aria-label), then the last `trailing` crumbs. Configurable via new optional `ShellPageHeaderConfig.breadcrumbCollapse?: { leading?; trailing? } | false`. Rebuild `dist/`; verify on the demo's nested-pages route (its 4-level chain triggers collapse). Full plan: `docs/2-tasks/T014-breadcrumb-overflow-collapse/task.md`. | `src/app-shell/ShellPageHeader.tsx` (collapse render + truncation classes + overflow dropdown), `src/app-shell/app-shell.css` (`.mrs-breadcrumbs*` truncation + overflow-item CSS), `src/app-shell/shellContract.ts` (`breadcrumbCollapse` config), `dist/` (rebuild), `docs/specifications/api-reference.md` (config row), `docs/guides/app-shell.md` (breadcrumb overflow section); verified in `my-react-shell-demo` nested-pages route. |
| T013 | vendor-themes-into-shell | in-progress | main | **Filed 2026-06-22.** Rearchitect distribution so a consumer (evaluering, the demos) depends on **only `my-react-shell`** — never the transitive `themes` git-dep. **Cause of the prod-parity pain:** `themes` shipped as a *second, independently-tagged* Bitbucket git-dep every consumer resolves transitively → CI must auth to two repos, two tags bumped in lockstep, and shell + themes are *contract-coupled but versioned separately* so they drift incompatible — the live bug: shell `v0.2.0`/`v0.3.0` consume the D15 surface tokens (`--color-surface-raised/sunken/sunken-deep`) but pin `themes#v0.1.0`, which defines only the *old* tokens, so a **real git-dep install renders every surface transparent** (the link symlink masks it in dev). **Fix:** `themes` becomes a **devDependency** of the shell; a `sync:themes` step **vendors** the curated palette CSS into committed `src/themes/*.css` (shipped via the existing `src/**/*.css` files glob); `src/index.css` imports them **relatively**. Dev liveness preserved with **no tagging**: `build:lib:watch` (the `dev start` `rs:watch` sidecar) also mirrors the themes checkout → `src/themes/` on save, so a themes edit HMRs into link consumers as today. A pre-commit **themes-freshness guard** (sibling of the dist guard) keeps `src/themes/` == resolved themes. Ships a **`release` script per repo** (themes → shell → evaluering) whose preflight makes the chain unfailable (shell refuses to release unless the vendored themes is clean + pushed + at the pinned tag), plus `docs/guides/release-runbook.md`. Parity gate = Vercel preview (the real pinned-tag build) before promote. | my-react-shell: `package.json` (themes deps→devDeps + tag bump, `sync:themes`/`release` scripts, `build:lib:watch` rewrite), `scripts/sync-themes.mjs` + `scripts/dev-watch.mjs` + `scripts/release.mjs` (new), `src/themes/*.css` (new, vendored), `src/index.css` (relative imports), `.githooks/pre-commit` (themes guard), `docs/guides/release-runbook.md` (new) + `distribution-model.md` + `strategy.md` (D5/D6/D13); `themes` repo (`scripts/release.mjs`); `evaluering` (`scripts/release.mjs` + prod git-dep switch). |
| T012 | color-picker-component | finished | main | **Filed 2026-06-22.** New opinionated kit component **`ColorPicker`** (`my-react-shell/components`) — a general, compact popover-trigger color picker. **Free** by default (full hue/saturation range; `onChange` emits in `format` — `hex`/`rgb`/`hsl`); pass a **`colors`** set to **constrain** it to a `Tab`-navigable swatch grid (entries may be any CSS color string). Controlled — `value`/`onChange` is a directly-usable CSS color string; persists nothing. Full-range mode uses **`react-colorful`** (tiny, zero-dep, MIT; **added as an optional peer of `/components`**, user-approved) rather than hand-rolling the saturation canvas; the swatch grid + popover stay shell-owned (Radix Popover, already an optional peer). *(An initial cut tied swatches to the shared theme accent tokens — dropped: the accent tokens don't read like the active palette and only 5 are contract-guaranteed, so they showed empty cells. Theme-token support removed; constrain via `colors` instead.)* Showcased on the demo's `/inputs-actions` page (free / constrained-set / rgb+hsl formats), `color-picker` icon registered in the demo `ICONS`/`EMOJIS` maps. | `src/components/ColorPicker.tsx` (new), `src/components/components.css` (`.mrs-color-picker*`), `src/components/index.ts` (export), `package.json` (react-colorful: devDep + optional peer), `docs/specifications/api-reference.md` (component row + `ColorPicker` subsection + peers row + surfaces map); `my-react-shell-demo` `src/pages/InputsActionsPage.tsx` + `src/shell-config.tsx`. |
| T011 | surface-token-model | finished | main | **Filed 2026-06-21.** Revisit the **semantic surface-token contract itself** (shared `themes` package) so depth is **mode-consistent and role-clear** in light *and* dark — the contract-level follow-on to **T010** (which fixed kit-local elevation geometry + the card accent against the *existing* ladder, and deliberately left the shared contract untouched). Verifying T010 on the new `my-react-shell-theme-demo` exposed a structural flaw: `surface-secondary`/`surface-tertiary` are documented as recessed "wells/insets" but are **darker than the card in light (recessed ✓) yet lighter than the card in dark (raised ✗)** — the same token inverts its depth meaning between modes, so a well/input reads inset in light and popped in dark. Also `surface-elevated` == `surface-primary` (`#ffffff`) in light (a popover is indistinguishable from a card except a near-invisible `0 1px 2px /.05` shadow), and `dynamic` uses a pure-`#000` base (OLED smear/halation, no headroom to recess). Grounded in conventions (Material 3 tonal overlays on a `#121212` base; Atlassian `surface.sunken` for recessed; Radix 12-step roles, same direction both modes; ~5–6 perceptible levels; alert = same-plane tint, not an elevation step; desaturate dark accents 15–25%; 4.5:1 text on every surface, both modes). **W1** decide+implement a mode-consistent model (recommended: Atlassian/Radix-style `background → surface → surface-raised → surface-sunken` role set, wells darker in dark); **W2** light-mode elevation (differentiate raised from card); **W3** palette-wide colour fixes across all six palettes (dynamic off pure black, desaturate dark accents, WCAG audit); **W4** theme-demo upgrade (light+dark ladders + raised/sunken row). **Shared-`themes` work → ripples to the SolidJS foundation (D13); a token add/rename must reach every consumer's CSS barrel + theme registry + the demo's `PALETTE_GROUPS` (theme-tokens skill); W1 needs a user decision before implementing.** Full plan: `docs/2-tasks/T011-surface-token-model/task.md`. | `themes/contract.css` (surface-token model — add/rename `surface-raised`/`surface-sunken`), all palette files (`ocean`/`forest`/`sunset`/`soft`/`dynamic`/`golden`.css — surface values both modes, dynamic base off `#000`, dark-accent desaturation), `themes/palettes.json`; my-react-shell `src/index.css` + theme registry + `docs/guides/theme.md` (revised ladder) if tokens rename; `my-react-shell-demo` `PaletteReference` `PALETTE_GROUPS`; `my-react-shell-theme-demo` Surfaces page; SolidJS foundation consumer-side (separate repo). |
| T010 | kit-elevation-surface-system | archived | main | **Filed 2026-06-21.** Make the kit's **cards and other components read as lifted layers** that play well with the theme surface ladder, instead of sitting flat on the page — plus tighten **which surface token each component uses for which role** (currently feels arbitrary). Driven by a measured gap vs the kons-ev reference (the look the user wants): kons-ev floats panels with a soft ambient shadow (`0 8px 30px rgba(0,0,0,.45)`), a visible lighter border, 12px radius, and a 4px colored gradient accent stripe per card type; the kit's `PhiCard`/`StatCard` use only `shadow-sm` (`0 1px 2px`, invisible on a dark page) and colour lives only in the icon. **Workstream A — elevation:** real shadow *geometry* (not just T004's dark shadow-token opacity bump) tuned per surface role, on cards + floating chrome; add an optional **accent** affordance (top stripe / left border → semantic hue) to `PhiCard`/`StatCard`, opt-in + non-breaking. **Workstream B — surface discipline:** audit `--color-surface-*` usage per component and document the ladder semantics (background-primary page → surface-primary card → surface-elevated popover/lift → surface-secondary muted inset → surface-tertiary accent/hover) in the theme + component-kit guides, then align components. The Ocean dark palette has separately been retuned to the kons-ev surface values (closes the palette half; this task closes the component half). Verified visually in the new `my-react-shell-theme-demo` (Surfaces page) + the existing kit pages, light+dark, multiple palettes. Full plan: `docs/2-tasks/T010-kit-elevation-surface-system/task.md`. | `src/components/*` (PhiCard, StatCard, Alert, InfoBox, Table, … elevation + accent + surface-token roles), `src/components/components.css` (shadow/accent + surface-role CSS), possibly `themes/contract.css` + palettes (if an elevation token is added — D-A, TBD), `docs/guides/theme.md` + `docs/guides/component-kit.md` (surface-ladder semantics), `docs/specifications/api-reference.md` (accent props); verified in `my-react-shell-theme-demo` + `my-react-shell-demo` kit pages. |
| T009 | dynamic-leaf-breadcrumb | finished | main | **Filed 2026-06-21.** App-shell **port regression** found while building the nested-pages demo in `my-react-shell-demo`: the React `findActiveChain` (`src/app-shell/ShellPageHeader.tsx`) gates its loop on static `subPages` only (`while (candidates.length > 0)`), so a **dynamic-only leaf** — a `useDynamicPages` child whose parent route has no static `subPages` — is silently dropped from the breadcrumb (the guide's own `useDynamicPages({ parent: '/sites' }) → /sites/$siteId` example is currently broken). The SolidJS `foundation` original keeps descending whenever `parentKey in dynamicByParent`, exiting only when the parent has neither static children nor a dynamic registration. Same file's dev "no registered page" warning also dropped foundation's two exemptions (`leaf.route in dynamicPages`, and `!leaf && pathname === '/'`), so a legitimate chrome-only header at `/` (no `/` page) falsely warns on every home visit — now **structural** since `40f5fd1` makes registering `/` throw, so `/`'s chain is always empty (this task's dev-warn fix is `40f5fd1`'s complement). Fix = port the two foundation snippets (loop continuation + dev-warn exemptions), no public-API change, then `pnpm build` (the demo link-consumes `dist/`). Reproduced against the installed `dist` algorithm (3 cases); both `useDynamicPages` examples in the current guide are affected. Full plan: `docs/2-tasks/T009-dynamic-leaf-breadcrumb/task.md`. | `src/app-shell/ShellPageHeader.tsx` (`findActiveChain` loop + dev-warn exemptions), `dist/` (rebuild), `docs/guides/app-shell.md` (dynamic-pages wording if it implies a static-sibling requirement); verified via `my-react-shell-demo` nested-pages page (consumer, follow-up). |
| T008 | phi-card | in-progress | main | **Filed 2026-06-20.** Port the SolidJS `foundation` **`PhiCard`** (golden-ratio–locked card, `src/kit/shared/PhiCard.tsx`) into the opinionated kit as `my-react-shell/components` → `<PhiCard>`, with the same API and φ-geometry: outer W:H = φ:1, bands φ:1, upper columns 1:φ, lower columns φ:1; four slots (`upperLeft`/`upperRight`/`lowerLeft`/`lowerRight`) plus `size` (sm/md/lg/xl = 180/240/320/480px), `leftBorderColor`, `dividers`, `centerContent`, `singleBand`, `onClick`/`hoverable`, and the exported `PHI` constant. Adapted to kit conventions: **self-contained surface** (the React kit ships no `Card`, so `PhiCard` renders its own bordered/elevated surface) and **own `mrs-`-prefixed CSS** in `components.css` (no Tailwind utilities) with the φ splits as `fr`-unit grid templates. Rule-of-two: the component already exists in the Solid foundation; this is its second ecosystem. Visual φ-geometry verification + showcase land in `my-react-shell-demo`'s Kit page (separate follow-up — no showcase in this repo, D7). Full plan: `docs/2-tasks/T008-phi-card/task.md`. | `src/components/PhiCard.tsx` (new), `src/components/components.css` (PhiCard block), `src/components/index.ts` (exports), `docs/guides/component-kit.md` (Shipped-today line + Layout section); `my-react-shell-demo` Kit page (follow-up). |
| T007 | typed-i18n-keys | in-progress | main | **Filed 2026-06-20.** Lift compile-time key safety into the shell's i18n seam (`my-react-shell/i18n`), opt-in and **non-breaking** — every public type/hook gains a key type param defaulting to `string`, and the runtime (provider, context, pure translator, missing-key store) is **unchanged**. Today the seam is stringly-typed (`TFunction = (key: string) => string`); a typo / missing key is caught only at runtime via the dev `<MissingTranslationsOverlay>`. evaluering (already a `link:` consumer) independently solved this with a hand-rolled `DictKey` union + `en: Dict` parity, so the ecosystem's strongest i18n property lives in one consumer while the shared hub is weaker than what depends on it — any consumer adopting the shell seam would *lose* compile-time safety. Make typed keys the shell standard: ship `createTypedI18n<K>()` (a typed factory — pure typing sugar over generic `useTranslation`/`translateNow`, **no new runtime/context**), make `TFunction`/`I18nContextValue`/`useTranslation`/`translateNow` generic over `K extends string = string`, and export a `DotPaths<T>` utility so nested-catalog consumers can derive the union. A consumer declares `const { useTranslation, translateNow } = createTypedI18n<DictKey>()` once and every call site is typed with zero per-call ceremony. **Supersedes the evaluering Option-B shim plan** — evaluering binds its `DictKey` to the shell hook directly (no local casts), keeping its **64 `useT()` + ~10 `useLanguage`/toggle sites untouched**; that adoption is consumer-side, **filed as evaluering T032** (blocked on this shipping). Sub-decision **decided 2026-06-20 → A** (factory binds a key union; `DotPaths<T>` exported for nested catalogs); new decision **D14**. Full plan: `docs/2-tasks/T007-typed-i18n-keys/task.md`. | `src/i18n/`: `i18nContext.ts` + `useTranslation.ts` + `translateNow.ts` + `translate.ts` (generics + `DotPaths<T>`), **new** `createTypedI18n.ts`, `index.ts` exports, `I18nProvider.tsx` (optional `messages` typing); typed-key `*.test-d.ts`; `docs/guides/i18n.md` (Typed keys section); `react-translation-rules` skill; `strategy.md` (D14); `concept.md`; `CLAUDE.md` (if seam desc changes); `my-react-shell-demo` i18n showcase. **Consumer-side (separate repo/task):** evaluering `src/i18n/index.ts`. |
| T006 | shared-theme-package | in-progress | main | **Filed 2026-06-20. Code complete + verified locally; committed in all 3 repos** (my-react-shell `tsc -b` + Vite build green — palettes resolve from `themes`, `golden` correctly excluded; foundation `tsc` green + all 7 `@import`s resolve; pre-commit guard passed). **Pending user:** create the Bitbucket remote, then push + **tag `themes` `v0.1.0`** and `pnpm install` in each consumer (until then the committed git-dep specifier resolves only via the local symlink). End theme/color drift between my-react-shell (React) and `zingularis/foundation` (SolidJS) by extracting the semantic token contract + palettes into a new **framework-neutral git-dep package `themes`** (`~/Developer/themes`, `git@bitbucket.org:kesteinbakk/themes.git`) that both consume. Ships `contract.css` (the superset `:root` `--color-*` contract, incl. the `-border`/`share-border` tokens foundation's `:root` was missing), the 5 shared palettes (`ocean/forest/sunset/soft/dynamic`, already byte-identical across both repos) + `golden`, an `index.css` barrel, and a `palettes.json` manifest — **pure CSS/JSON, no build**. Both repos `@import` from `themes` and add it as a regular `dependencies` git-dep (transitive to their own consumers); each keeps its own framework-specific `ThemeProvider` and its own small baseline element styles (foundation's are tangled with app utility classes + fretboard animations, which stay put). Selected approach **A** over a sync-script (B). Supersedes **D4** ("foundation untouched") via new **D13**. Distribution = Bitbucket git-dep per D5; local dev via symlink/link, the committed specifier is the tag-pinned git-dep. **Remote creation + push/tag = user-owned.** Full plan: `docs/2-tasks/T006-shared-theme-package/task.md`. | New repo `~/Developer/themes` (`contract.css`, 6 palettes, `index.css`, `palettes.json`, `package.json`, `README.md`); my-react-shell `src/index.css` + `src/styles/base.css` (split contract out) + delete `src/styles/themes/*` + `package.json` (dep + `files`) + `strategy.md` (D13) + `concept.md` + `CLAUDE.md` + memory; foundation `src/themes/*` (base.css split, swap palette imports) + `package.json`. |
| T005 | user-preferences | in-progress | main | **Filed 2026-06-20. Code complete** — lib + demo typecheck green, committed; **pending** in-browser verification (modal opens, palette/mode change live, emoji toggle flips every shell icon — user-owned dev server). A default **user-preferences** component for the kit plus an **icon/emoji display-mode** seam, inspired by foundation's `ThemeAction` modal (`shell/actionRow.tsx`) + `kit/icons/Icon.tsx`. New module `my-react-shell/icons`: `IconModeProvider` (uncontrolled+localStorage by default, controllable via `value`+`onChange` to redirect to a backend), `useIconMode`, and a thin `<Icon icon emoji>` swap that renders an SVG glyph or its emoji per the active mode — **no baked-in registry and no `lucide-react` dep** (consumer brings keys; the demo builds a `renderIcon` registry on top). New component `my-react-shell/components` → `<UserPreferences>`: a **controlled** theme/display panel (palette grid + light/dark/system + icons↔emojis) in a Radix **dialog** (reuses the existing `@radix-ui/react-dialog` peer — no popover dep), exposing the value + `onChange` for every preference so the consumer owns persistence (localStorage / Convex / account); optional `accountActions` slot; labels via props (no runtime i18n); auth-free + Convex-free. Additive non-breaking `onChange` on `ThemeProvider`. Demo: `IconModeProvider` wrap, chrome swaps its `PaletteSelect`/`ThemeModeToggle` for `<UserPreferences>`, and `renderIcon` honors the mode so the whole demo flips icons↔emojis. **Decisions:** Q1→seam+thin-Icon (not a full Icon module); Q2→preferences-only (auth via optional slot); component is controlled (project chooses storage). Full plan: `docs/2-tasks/T005-user-preferences/task.md`. | `src/icons/` (new), `src/components/UserPreferences.tsx` + `components.css` + `index.ts`, `src/theme/ThemeProvider.tsx`, packaging (`exports`, `CLAUDE.md`), `concept.md`, `strategy.md` (D12), `docs/guides/` (new icons guide + component-kit entry); demo `main.tsx`, `chrome.tsx`, `shell-config.tsx`, `pages/`. |
| T004 | component-kit | finished | main | **Filed 2026-06-20.** **Phases 1–4 done** (charter D11 + `components` module; P1 `Alert`; P2 feedback: `Spinner`/`PageSpinner`/`SectionSpinner`, `InfoBox`, `EmptyState`, `ConfirmDialog`, `Toast`; P3 data-display: `Badge`, `Chip`/`ChipGroup`, `Avatar`/`AvatarGroup`, `Table` — `Card` left to shadcn per the rule; P4 forms: `InputField`, `SegmentedControl`, `Select` (+ `@radix-ui/react-select` optional peer); plus the dark-shadow-token elevation fix; all in the demo Kit route; lib+demo typecheck green). **Closed complete** — the app-shell CVA-drift align was declined (cosmetic; would couple app-shell→components or duplicate the helper). Ship an **opinionated component kit** at `my-react-shell/components` — React composites that bake design/layout/behavior decisions on shadcn/Radix primitives and render against the semantic theme tokens (light+dark, every palette), mirroring `@foundation/kit`. **Reverses the "no component kit" charter** (concept "What it is NOT", strategy D3/D8, CLAUDE.md) via a new decision D11, following the D10 app-shell precedent (component module behind a sub-path with optional Radix peer). **Scope line:** ship only components that need an *opinion*; un-opinionated shadcn primitives (Button/Input/Checkbox/…) stay consumer-owned and are shown as demo examples only. Proposed set: Alert, ConfirmDialog, Toast, InfoBox, EmptyState, Spinner, Card, Badge, Chip, Avatar, Table, InputField, Select, SegmentedControl — phased, **Alert first**. All showcased in a new **Kit route** in `my-react-shell-demo` (landing card + page + nav, mirroring the existing Components/Palette pages). Optional peers (Radix, CVA, tailwind-merge, clsx) approval-gated at install. Full plan: `docs/2-tasks/T004-component-kit/task.md`. **Refs:** foundation `src/kit/`, strategy D3/D8/D10, the secondary+`-strong` token work. | `src/components/` (new), packaging (`exports`, optional peers, `tsconfig.lib.json`), `concept.md`, `strategy.md`, `CLAUDE.md`, `README.md`, `docs/guides/component-kit.md` (new); demo `pages/KitPage.tsx`, `router.tsx`, `HomePage`, `shell-config.tsx`, `sections/`. |
| T003 | app-shell-module | in-progress | main | **Filed 2026-06-17.** React (SPA) port of the SolidJS `foundation` app shell as an optional `my-react-shell/app-shell` module. Ships the shell chrome (`AppShell` header-or-sidebar mode + responsive mobile drawer + optional bottom nav + the single scrolling body cell), `ShellPageHeader` (URL-derived breadcrumbs + actions + search + `subPages` title dropdown), `defineShellConfig` (pages tree + the three navigation layers), and the page-tab primitives (`PageSections` in-page `?tab=` with single/list-scrollspy/collapsible + route-based `PageTabs`). **TanStack Router** is an *optional* peer behind the sub-path (amends D2); **Radix** headless primitives for Sheet/Drawer, DropdownMenu, Popover. Display strings via config/props — no hard i18n dep. Excludes app-glue (notification system, feedback modal, command-bar/action registry). From the approved proposal `docs/1-proposals/app-shell-module.md`; sequenced **after** the i18n module. **Code complete** — all phases implemented (spine + chrome + page header + page tabs + packaging + guide); `tsc -b` + `pnpm build:lib` green, 13 config-validator checks pass. **Pending:** browser-behavior verification + harness playground routes (a new route needs a dev-server run to regenerate `routeTree.gen.ts` — user-owned). **Refs:** foundation `src/shell/` + `docs/guides/app-shell-rules.md`, strategy D2/D8/D9. | `src/app-shell/` (new), packaging (`exports`, optional peers), `docs/guides/app-shell.md` (new), harness routes `src/routes/**`, `concept.md`, `strategy.md`. |
| T002 | dedupe-link-dev-loop | finished | main | **Filed 2026-06-16.** De-risk the `link:` dev-loop against duplicate-React crashes. A consumer following the documented local `link:` loop hits a hard `Invalid hook call` crash at first paint: the symlinked shell drags in its own `node_modules/react`, so the consumer's bundler resolves two physically distinct React copies and the hook dispatcher splits. Fix is consumer-side bundler dedupe (Vite `resolve.dedupe`), baked into the reference consumer configs and documented next to the existing peers / Tailwind / strip-the-link steps. The shell keeps its own React (dev-harness/build/tests need it), so this is document-and-encode, not package-level elimination. Scoped to the `link:` loop only — the tag-pinned git-dep path dedupes via peers and is unaffected. From `docs/1-proposals/react-dedupe-link-dev-loop.md`. | `docs/guides/distribution-model.md`, `README.md`, `CLAUDE.md`; consumer configs `evaluering/vite.config.ts`, `my-react-shell-demo/vite.config.ts`. |
| T001 | modular-foundation | in-progress | — | **Filed 2026-06-15.** Stand up my-react-shell as a **modular React foundation**: optional, self-contained **drop-in modules** (theme, providers, auth seam, i18n) an app imports à la carte, consumed like a standard npm package from a tag-pinned Bitbucket git-dependency (a `prepare` build compiles `src/ → dist/`). **No** component kit, **no** shadcn registry, **no** mandated app-shell. Each module is independently importable, self-contained, and ships a contract + guide; modules contributed on rule-of-two reach every app on version bump. Done: scaffold, theme, providers + auth seam, distribution, **i18n module** (`my-react-shell/i18n`), **module-authoring + per-module guides**, `core` left empty-by-design (rule-of-two for types). Remaining: the v0.1.1 distribution zero-config-install decision (build sequence D2 item 5 — owner call) + harness showcase routes (need a dev-server run to regenerate `routeTree.gen.ts`). Supersedes the original "port foundation-react / host a shadcn registry / build the app-shell" scope (strategy D8). Blocks every my-react-shell consumer. **Refs:** strategy D8, react-framework-notes. | `src/theme/`, `src/providers/`, `src/auth/`, `src/core/` (new), `src/i18n/` (new), packaging (`tsconfig.lib.json`, `exports`), `docs/guides/`. |
