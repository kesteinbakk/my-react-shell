# Away report — 2026-06-23 — T018 complete-component-surface

Autonomous run (`/independent-work`). Branch `claude/zen-shannon-7abaec`.

## Completed

- **W1 — doc re-founding.** Deleted `docs/strategy.md` (the Dx rule log); rewrote
  `docs/concept.md` to the *support & starting base for React* thesis (React counterpart
  to the SolidJS `foundation`); created the T018 task doc.
- **W2 — reference purge.** Swept every live doc — `CLAUDE.md`, `README.md`, `demo.md`,
  `guides/theme.md` (removed the shadcn cssVar-bridge section), `guides/distribution-model.md`
  (purged Dx cites), `specifications/api-reference.md`, and the `my-react-shell` skill card
  (both worktree + primary copies; `.claude/` is gitignored). Grep-verified: zero dangling
  `strategy.md` / `Dx` / shadcn-direction refs in live docs. *(commit `f7f33a3`)*
- **W4 — tone/variant cleanup.** Canonical `Tone` + `TONE_COLOR` (`src/components/tone.ts`);
  `accent.ts` uses it; `StatCard`/`PhiCard` `AccentTone`→`Tone`; `Alert`/`ConfirmDialog`/
  `ActionButton` `variant`→`tone` (`AlertVariant`→`AlertTone`, `ActionButtonVariant`→
  `ActionButtonTone`, dropped the unused `secondary`); `Toast` follows `AlertTone`; `Badge`
  gained `primary` (solid pill); `Collapsible`/`Accordion` keep `variant` (genuinely
  structural). api-reference updated; dist rebuilt; lib typecheck clean. *(commit `7e78155`)*
- **W3 — primitives (no new deps).** `Button` (variant × tone × size via a CSS-var matrix),
  `Input`, `Textarea`, `Label`, `Card` (+Header/Title/Description/Content/Footer), `Separator`,
  `Skeleton`, `Dialog` (general, on the existing `@radix-ui/react-dialog` peer), `Popover`,
  `DropdownMenu` (both on existing Radix peers). All on Radix + theme tokens, `mrs-` CSS,
  a11y, light+dark. api-reference rows added; dist rebuilt; lib typecheck clean; **every
  CSS `--color-*` / `--mrs-elevation-*` token validated against the contract.**
  *(commits `dbc63c7`, `5839f39`)*

## Remaining + carved-out follow-ups

- **W3 Radix-peer primitives — BLOCKED on a dependency grant.** `Checkbox`, `Switch`,
  `RadioGroup`, `Tooltip`, `Tabs` each need a **new `@radix-ui/*` peer**
  (react-checkbox / switch / radio-group / tooltip / tabs). Dependency changes need your
  approval (root CLAUDE.md), so I stopped short. Once approved, build them in the shipped
  idiom. *(Checkbox/Switch/RadioGroup/Tabs could alternatively be built native-without-Radix
  — but that's an approach-switch from the agreed plan, so it also needs your nod.)*
- **W5 — demo (`my-react-shell-demo` repo).** Showcase the new primitives + the tone renames
  on the kit pages; convert the `/components` route into a kit-primitives showcase. Separate
  repo; benefits from in-browser verification (not started autonomously).
- **W6 — evaluering migration.** Drop `components/ui` (shadcn) + the cssVar bridge, repoint
  imports to `my-react-shell/components`, migrate the tone renames. Separate repo; depends on
  a shell tag bump.
- **react-framework skill.** Still runs `shadcn init` / `add` for new projects — update to use
  `my-react-shell/components` once the surface is tagged (already recorded in T018 W2).

## Concerns found mid-work

- **`primary` lacks a tint token set.** Unlike info/success/warning/danger (which ship
  `-bg`/`-border`/`-strong`), `primary` only has `--color-primary` + `-bg`/`-content`/`-hover`.
  Handled: Badge `primary` is a solid pill; Button's soft/outline/ghost use `--color-primary`
  as the tint foreground (saturated-on-tint — legible but not `-strong`-backed). If you want a
  softer primary, add `--color-primary-strong`/`-border` to the `themes` contract.
- **`pnpm typecheck` (tsc -b, includes the harness)** reports 2 errors from a missing
  `src/routeTree.gen.ts` (generated by the dev server; git-ignored). **Pre-existing** — present
  on HEAD, unrelated to this work. The library typecheck (`tsc -p tsconfig.lib.json`) is clean.

## Suggested cleanup

- `ConfirmDialog` still renders its own `mrs-dialog__btn` buttons; now that `Button` ships it
  could use `<Button>` internally (cosmetic; left as-is, out of scope).

## Dev env / file changes

- None. No env vars, no `.env*` edits, no deployments, no dev servers started, **no
  dependencies added.**

## Recommended next step

Approve the 5 new `@radix-ui` peers (or OK a native implementation) so W3's remaining
primitives land; then W5 (demo showcase). A shell tag bump after that unblocks W6 (evaluering).
