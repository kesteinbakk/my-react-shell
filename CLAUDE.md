# CLAUDE.md — my-react-shell

Agent instructions live in **[`AGENTS.md`](AGENTS.md)** — the single source read by
every agent tool. This file imports it so Claude Code loads the same rules. **Edit
`AGENTS.md`; never mirror content back here.**

@AGENTS.md
