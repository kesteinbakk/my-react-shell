# Agent instructions (non-Claude tools)

**If you are a Claude agent, ignore this file** — your instructions load through
`CLAUDE.md`. This file is for Codex / Cursor / Gemini / Antigravity and any other
tool that reads `AGENTS.md`.

Before starting work, read these (each exists in this project unless noted):

1. `docs/work-contract.md` — universal rules.
2. `docs/concept.md` — this project's concept, and your **default role** (stated on a `Default role:` line). Adopt that role unless the user tells you otherwise.
3. Your role's guides: **every file** in `docs/guides/general/` and in `docs/guides/<your-role>/` (either directory may be missing or empty — that's fine).
4. `docs/agents/skills.md` — the skill index. **Hard rule: before starting a task, read the relevant skill(s) listed there.**

`docs/specifications/` holds reference facts (not required reading). Search it when
you need detail, and to avoid re-deriving things already documented.

<!-- DO NOT EDIT. Synced entry file — byte-identical in every project, regenerated by the agent-docs sync. It holds no rules of its own; it only points at the files above. Change the template in dev-docs/agent-source/AGENTS.md. -->
